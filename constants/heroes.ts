import type { Universe, PowerType } from '@/types/hero'

export const UNIVERSES: Universe[] = ['Marvel', 'DC', 'Other']

export const POWER_TYPES: PowerType[] = ['Strength', 'Speed', 'Intelligence', 'Magic']

export const REVALIDATE_TIME = 3600
